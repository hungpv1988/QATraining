@model StudentAppMvc.Models.ViewModels.SubjectViewModel

@{
    ViewData["Title"] = "Create Subject";
    SelectList sl = new SelectList(Model.AvailableTeachers, "Id", "Name");
    var myListItems = new List<SelectListItem>();

    myListItems.AddRange(Model.Teachers.Select(keyValuePair => new SelectListItem()
{
    Value = keyValuePair.Key.ToString(), 
    Text = keyValuePair.Value
}));
var myList = new SelectList(myListItems);
}

<h1>Create Subject</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
                <div class="form-group">  
                    <label asp-for="AvailableTeachers" class="control-label"></label>
    <select asp-for="AvailableTeachers" asp-items="sl" size="10" multiple class="form-control"></select>  
                @*@Html.DropDownList(
                    "AvailableTeachers",
                    new SelectList(
                        Model.AvailableTeachers.Select(x => new { Value = x.Id, Text = x.Name }),
                        "Value",
                        "Text",
                        new { @style = "width:500px" }
                        )
                    )*@
                @*@Html.DropDownListFor(x => x.AvailableTeachers, new SelectList(Model.AvailableTeachers), "Select a teacher...", new { @style = "width:500px" })*@
            </div>
            <div class="form-group">  
                    <label asp-for="Teachers" class="control-label"></label>
    <select asp-for="Teachers" asp-items="myList" size="10" multiple class="form-control"></select> </div>
            <br />
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<br />

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
}
